---

- name: Install open-iscsi,nfs-common using apt
  ansible.builtin.apt: 
    name: 
      - open-iscsi
      - nfs-common
    state: present 

- name: start and enable iscsid
  ansible.builtin.systemd:
    name: iscsid
    state: started
    enabled: yes